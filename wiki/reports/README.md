# Wiki Reports

This directory stores generated maintenance output from `npm run wiki:maintain`.

- `latest-maintenance.json` is the most recent queue + lint run.
- `latest-lint.json` is the most recent lint-only snapshot.

The JSON report files are intentionally ignored by git.
